var minhaVar = "Minha variável";

function myFunc(x, y)){
    return x + y;
}

// es6 ou ES2016
let num = 2;
const PI = 3.14;

var numeros = [1,2,3];

numeros.map(function(valor){
    return valor *2;
});

numeros.map(valor => valor *22);